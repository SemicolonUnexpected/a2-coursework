name: WinForms Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore src/MyWinFormsProject.sln

      - name: Build
        run: dotnet build src/MyWinFormsProject.sln --configuration Release --no-restore

      - name: Run tests
        run: dotnet test src/MyWinFormsProject.sln --configuration Release --no-build --verbosity normal

      - name: Publish
        run: dotnet publish src/MyWinFormsProject/MyWinFormsProject.csproj --configuration Release --output ./publish

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: winforms-app
          path: ./publish
